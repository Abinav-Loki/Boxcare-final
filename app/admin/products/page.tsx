import { RoutePlaceholder } from "@/components/common/route-placeholder";

export default function AdminProductsPage() {
  return (
    <RoutePlaceholder
      owner="Developer B"
      title="Admin Products"
      description="Product CRUD with images, category, dimensions, variants, pack prices, stock, status, and SEO fields."
    />
  );
}

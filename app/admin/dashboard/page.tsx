import { RoutePlaceholder } from "@/components/common/route-placeholder";

export default function AdminDashboardPage() {
  return (
    <RoutePlaceholder
      owner="Developer B"
      title="Admin Dashboard"
      description="Operational dashboard for orders, revenue, pending payments, processing, shipped orders, and low stock."
    />
  );
}

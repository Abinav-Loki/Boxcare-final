import { RoutePlaceholder } from "@/components/common/route-placeholder";

export default function AdminLoginPage() {
  return (
    <RoutePlaceholder
      owner="Developer B"
      title="Admin Login"
      description="Protected admin authentication entry. Add credentials/session handling before exposing production admin routes."
    />
  );
}

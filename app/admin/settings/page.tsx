import { RoutePlaceholder } from "@/components/common/route-placeholder";

export default function AdminSettingsPage() {
  return (
    <RoutePlaceholder
      owner="Developer B"
      title="Admin Settings"
      description="Store contact, WhatsApp number, GST message, shipping rules, social links, SEO defaults, and feature toggles."
    />
  );
}

import { RoutePlaceholder } from "@/components/common/route-placeholder";

export default function CustomBoxesPage() {
  return (
    <RoutePlaceholder
      owner="Developer A"
      title="Custom Box Builder"
      description="Migrate the finalized calculator and six-step custom quote builder as a quote/enquiry workflow."
    />
  );
}
